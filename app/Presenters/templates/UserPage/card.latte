{block content}
    <div class="card" id="userCard" style="width: 18rem; position: relative; z-index: 1070; display: none;position: fixed;
    right: 20px;">
        <img src="#" class="card-img-top">
        <div class="card-body">
            <h5 class="card-title">{$userName}</h5>
            <p class="card-text">{$userEmail}</p>
            <a n:href="Sign:out" class="btn btn-primary">Выйти</a>
        </div>
    </div>
{/block}